import { Stack } from "@mui/material";

const Footer = () => {
  return (
    <Stack
      sx={{
        width: "100%",
        height: "100px",
        backgroundColor: "#000",
        color: "white",
        fontSize: "2rem",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Footer
    </Stack>
  );
};

export default Footer;
